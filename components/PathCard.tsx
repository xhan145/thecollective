import Link from "next/link"; import { ArrowRight } from "lucide-react"; import type { GrowthPath } from "@/lib/types";
export function PathCard({path}:{path:GrowthPath}){return <Link href={`/paths/${path.slug}`} className="card block p-4 active:scale-[0.99]"><div className={`mb-4 h-12 w-12 rounded-2xl bg-gradient-to-br ${path.color}`}/><div className="flex items-start justify-between gap-4"><div><h3 className="text-lg font-black">{path.title}</h3><p className="mt-1 text-sm text-slate-400">{path.description}</p></div><ArrowRight className="mt-1 text-slate-500" size={18}/></div><p className="mt-3 text-xs leading-5 text-slate-500">{path.promise}</p></Link>}
