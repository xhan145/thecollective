import { NextResponse } from "next/server"; import { z } from "zod";
const schema=z.object({proof:z.string().min(1), reflection:z.string().optional(), feedbackRequest:z.string().optional()});
export async function POST(req:Request){const body=await req.json().catch(()=>null); const parsed=schema.safeParse(body); if(!parsed.success) return NextResponse.json({error:"Invalid feedback request."},{status:400}); if(!process.env.OPENAI_API_KEY) return NextResponse.json({source:"demo-fallback",whatWorked:"You took a concrete step and reflected on it.",whatCouldImprove:"Make the next version more specific so another person can understand exactly what action you want to take.",nextStep:"Rewrite the proof as one clear sentence, then choose whether to keep it private or ask for human feedback.",reflectionQuestion:"What made this small action easier or harder than expected?"}); return NextResponse.json({source:"placeholder",whatWorked:"OpenAI key detected.",whatCouldImprove:"Wire this route to the Responses API when you are ready for live AI calls.",nextStep:"Keep fallback mode until your prototype flow works.",reflectionQuestion:"What feedback would be most useful?"});}
