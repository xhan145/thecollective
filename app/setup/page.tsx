import { AppShell } from "@/components/AppShell"; import { SetupChecklist } from "@/components/SetupChecklist"; export default function SetupPage(){return <AppShell title="Setup" subtitle="This app runs in demo mode first. Connect keys later."><div className="space-y-4"><div className="card p-5"><h2 className="text-xl font-black">Beginner mode is on</h2><p className="mt-2 text-sm leading-6 text-slate-400">You can preview the UI before Supabase or OpenAI are connected. When ready, add Secrets in Replit and run the SQL file in Supabase.</p></div><SetupChecklist/></div></AppShell>}
