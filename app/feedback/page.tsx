import Link from "next/link"; import { AppShell } from "@/components/AppShell";
export default function FeedbackPage(){return <AppShell title="AI Feedback" subtitle="Demo fallback feedback. Connect OpenAI later for live generation."><div className="space-y-4"><Feedback label="What worked" color="text-green" text="You were clear and direct. Your message names what you need without attacking the other person."/><Feedback label="What could improve" color="text-orange" text="Add a specific timeline or next action so the other person knows exactly how to respond."/><Feedback label="Next step" color="text-purple2" text="Say it out loud once without adding an apology before it. Notice where you want to soften the request."/><Link href="/dashboard" className="btn-primary w-full">Continue to dashboard</Link></div></AppShell>}
function Feedback({label,color,text}:{label:string;color:string;text:string}){return <div className="card p-5"><p className={`text-xs font-black ${color}`}>{label}</p><p className="mt-2 text-sm leading-6 text-slate-300">{text}</p></div>}
